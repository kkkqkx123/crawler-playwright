激活环境：.venv/Scripts/activate

环境配置历史：
已有playwright浏览器实例：
PLAYWRIGHT_BROWSERS_PATH = D:\Source\pw-browsers

uv pip install playwright
uv pip install pandas
uv pip install lxml【代替bs4】
uv pip install httpx
uv add openai